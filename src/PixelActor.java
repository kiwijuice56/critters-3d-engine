import info.gridworld.actor.Actor;

import java.awt.Color;

/**
 * Represents a pixel in a screen world
 */
public class PixelActor extends Actor {
	public PixelActor() {
		setColor(Color.BLACK);
	}
}
